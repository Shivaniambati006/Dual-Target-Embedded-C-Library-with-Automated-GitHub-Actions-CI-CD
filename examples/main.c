#include <stdio.h>
#include "mathlib.h"

int main()
{
    printf("Add: %d\n", add(10, 5));
    printf("Subtract: %d\n", subtract(10, 5));
    printf("Max: %d\n", max(10, 5));
    printf("Min: %d\n", min(10, 5));

    return 0;
}